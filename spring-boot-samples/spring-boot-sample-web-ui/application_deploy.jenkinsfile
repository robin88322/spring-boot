pipeline {

    agent any
    stages{
        stage(DEPLOY){
            steps{
                sh "ANSIBLE_HOST_KEY_CHECKING=False ansible-playbook  -i /var/lib/jenkins/workspace/integration/hosts /var/lib/jenkins/workspace/integration/playbook2.yml -e 'build=${env.select_build}'"
            }
        }
    }